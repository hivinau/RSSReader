<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tool="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tool:context=".activities.MainActivity"
    android:background="@color/background"
    android:id="@+id/feeds_coordinator_layout">

    <RelativeLayout
        android:id="@+id/welcome_relative_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:visibility="visible">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/add_feed"
            android:textColor="@color/add_feed"
            android:textSize="@dimen/add_feed"
            android:gravity="center"
            android:minLines="2"/>

    </RelativeLayout>

    <android.support.v7.widget.RecyclerView
        android:id="@+id/feeds_recycler_view"
        android:scrollbars="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="5dp"/>

    <!-- <android.support.design.widget.FloatingActionButton
        android:id="@+id/add_feed_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="16dp"
        android:src="@drawable/add"
        android:background="@color/float_button"
        app:backgroundTint="@color/float_button"
        app:layout_anchor="@id/feeds_recycler_view"
        app:layout_anchorGravity="bottom|right|end"/>
        app:layout_behavior="fr.unicaen.info.users.hivinaugraffe.apps.android.rssreader.views.ScrollAwareFABBehavior"/> -->

</android.support.design.widget.CoordinatorLayout>